apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-cronor
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cronimage
            image: cronor:0.0.1
            imagePullPolicy: Always
          restartPolicy: Never